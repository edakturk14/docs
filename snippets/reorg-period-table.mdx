export const InteractiveReorgTable = () => {
  const [activeTab, setActiveTab] = useState('mainnet');

return (

<div className="w-full not-prose">
<div className="border-b border-gray-200 mb-6">
<nav className="-mb-px flex space-x-8">
<button
onClick={() => setActiveTab('mainnet')}
className={`py-2 px-1 border-b-2 font-medium text-sm transition-colors ${
              activeTab === 'mainnet'
                ? 'border-blue-500 text-blue-600'
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`} >
Mainnet
</button>
<button
onClick={() => setActiveTab('testnet')}
className={`py-2 px-1 border-b-2 font-medium text-sm transition-colors ${
              activeTab === 'testnet'
                ? 'border-blue-500 text-blue-600'
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`} >
Testnet
</button>
</nav>
</div>

      <div className="p-4 bg-gray-50 rounded-lg">
        <p>Current tab: <strong>{activeTab}</strong></p>
        <p>Table will go here next...</p>
      </div>
    </div>

);
};

export default InteractiveReorgTable;

;
